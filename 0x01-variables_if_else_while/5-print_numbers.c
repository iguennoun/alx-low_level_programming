#include <stdio.h>
/**
** main - Program begening
**
** Return: 0 to stop the program
**
*/
int main(void)
{
	int dig;

	for (dig = 0 ; dig < 10 ; dig++)
		printf("%d", dig);
	putchar('\n');
	return (0);
}
